# Codex artifacts (legacy — generated by the old sync-codex.sh, no longer used)
.codex/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*~

# Per-repo runtime config (lives in user projects, never in this plugin repo)
.atdd-pipeline.json

# Scratch / experiments
scratch/
*.tmp
*.log

# Node leftovers, if anyone ever wraps the plugin in a JS package
node_modules/
